[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py

markers =
    knownbug: test targets a scenario the assignment brief requires; a failure is review evidence, not a test defect

addopts = -v --tb=short